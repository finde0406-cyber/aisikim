# AGENTS.md

## 목적
이 문서는 Codex, Claude Code 등 AI 에이전트가 `AI시킴` 프로젝트에서 공통으로 따라야 할 작업 규칙을 정의합니다.

## 최우선 원칙
- 코드 구현 전에 반드시 `docs/aisikim-prd-v1.md`와 `docs/aisikim-business-plan-v1.md`를 읽는다.
- 현재 단계가 문서 단계인지, 설계 단계인지, 구현 단계인지 먼저 확인한다.
- 사용자 승인 없이 commit, push, 배포, 패키지 설치, 프로젝트 생성, 파일 덮어쓰기를 하지 않는다.
- 명시되지 않은 범위의 기능을 임의로 추가하지 않는다.

## MVP 범위 준수
- 1차 MVP는 모바일 최적화 웹서비스 기준으로만 다룬다.
- 포함 기능: 랜딩페이지, 선택형 진단, 무료 결과, 이메일 CTA, 샘플팩 안내, 유료 스타터팩 판매 페이지, 외부 결제 링크 연결, 필수 고지문
- 제외 기능: 로그인, 회원가입, 자체 결제, 구독, 대시보드, 저장함, 관리자 페이지, 앱 출시
- "좋아 보인다"는 이유만으로 범위를 넓히지 않는다.

## 문서 작업 규칙
- 모든 문서는 한국어로 작성한다.
- "프롬프트"보다 "작업지시서" 표현을 우선 사용한다.
- AI 초보자도 이해할 수 있도록 쉽고 직관적으로 쓴다.
- 문서 간 표현과 정책이 충돌하지 않도록 용어를 통일한다.

## 구현 작업 규칙
- 구현 전 먼저 변경 계획을 요약하고, 어떤 문서를 기준으로 삼는지 밝힌다.
- 모바일 우선 UI를 기본 전제로 한다.
- 이메일 수집과 결제는 외부 서비스 연동 구조로 단순하게 설계한다.
- 개인정보 관련 입력 필드는 최소화한다.
- 보안과 개인정보 리스크가 있는 부분은 구현보다 먼저 검토 문서를 남긴다.

## 금지 사항
- 사용자 승인 없는 commit/push
- 사용자 승인 없는 패키지 설치
- 사용자 승인 없는 프로젝트 초기화
- 사용자 승인 없는 배포
- MVP 범위 초과 구현
- 동의 없는 이메일 수집 또는 과장된 마케팅 문구

## 작업 종료 시
- 어떤 문서를 기준으로 어떤 판단을 했는지 요약한다.
- 파일 변경 후에는 필요한 검증 결과만 간단히 보고한다.
- git 작업은 승인 범위 안에서만 수행한다.
