# CLAUDE.md

## 목적
이 문서는 Claude Code가 `AI시킴` 프로젝트에서 작업할 때 따라야 하는 전용 규칙을 정리합니다.

## 첫 작업 원칙
- 첫 작업에서는 절대 코드 수정부터 시작하지 않는다.
- 먼저 현재 문서 구조와 기준 문서를 읽고 설계 보고를 제출한다.
- 설계 보고에는 현재 이해한 목표, 구현 범위, 제외 범위, 필요한 파일, 예상 리스크를 포함한다.

## 코드 수정 전 필수 절차
- 어떤 파일을 왜 바꾸는지 먼저 계획을 보고한다.
- 변경이 `docs/aisikim-prd-v1.md`와 일치하는지 스스로 검토한다.
- 범위가 애매하면 확장하지 말고 MVP 안에서 가장 단순한 선택을 한다.

## MVP 준수 규칙
- 모바일 최적화 웹서비스 외의 플랫폼으로 확대하지 않는다.
- 자체 결제 시스템을 넣지 않는다.
- 로그인, 회원가입, 대시보드, 저장 기능을 임의로 추가하지 않는다.
- AI 자동 생성 로직을 과도하게 복잡하게 만들지 않는다.

## 사용자 승인 관련 규칙
- 사용자 승인 없이 commit/push 금지
- 사용자 승인 없이 패키지 설치 금지
- 사용자 승인 없이 배포 금지
- 사용자 승인 없이 프로젝트 생성 및 대규모 파일 생성 금지

## 문체 및 UX 규칙
- 쉬운 언어를 우선 사용한다.
- "프롬프트"라는 표현보다 "작업지시서"라는 표현을 우선 사용한다.
- 장난스럽기보다 직관적이고 믿을 수 있는 톤을 유지한다.
- 모바일에서 읽기 쉬운 정보 구조를 우선 고려한다.

## 산출물 보고 방식
- 변경 전: 계획 요약
- 변경 후: 무엇을 바꿨는지, 기준 문서와 어떻게 맞췄는지, 남은 리스크가 무엇인지 간단히 보고
- 구현 대신 설계 단계라면 문서 정합성과 범위 적합성을 중심으로 보고
